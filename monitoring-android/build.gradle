apply from: "$rootDir/gradle/kotlinAndroidLib.gradle"

android {
    kotlinOptions.freeCompilerArgs += ["-module-name", "monitoring.android"]
}

dependencies {
    api project(':monitoring')

    implementation Libs.crashlytics
}